{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "builds": [{ "src": "./index.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/products", "dest": "/" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
